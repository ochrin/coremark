addons:
  apt:
    update: true
    packages: git wget flex bison gperf python python-pip python-setuptools cmake ninja-build ccache libffi-dev libssl-dev dfu-util

before_script:
   - pushd ~
   - git clone --recursive --branch release/v4.2 https://github.com/espressif/esp-idf.git
   - cd esp-idf
   - ./install.sh
   - . ./export.sh
   - popd

language: minimal

script:
   - idf.py build
